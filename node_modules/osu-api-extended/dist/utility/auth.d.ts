import { auth_params, auth_response, auth_scopes, IError, lazer_auth_response, Modes_names } from '../types/index';
import { UserAuth } from '../types/v2/users_details';
export declare const settings: {
    timeout: number;
    throwErrors: boolean;
};
export declare const credentials: {
    type: any;
    api_key: string;
    client_id: number | string;
    client_secret: string;
    login: string;
    password: string;
    redirect_url: string;
    state: string;
    cachedTokenPath: string;
    scopes: auth_scopes;
};
export declare const cache: {
    v1: string;
    v2: string;
    'ratelimit-remaining': number;
    'ratelimit-limit': number;
};
type ResponseLogin<T extends auth_params['type']> = T extends 'v2' ? Promise<auth_response & IError> : T extends 'v1' ? string : T extends 'lazer' ? Promise<lazer_auth_response & IError> : T extends 'cli' ? Promise<auth_response & IError> : null;
export declare const login: <T extends auth_params>(params: auth_params) => ResponseLogin<T["type"]>;
export declare const set_v2: (token: string) => string;
export declare const refresh_token: () => Promise<string | (auth_response & IError) | (lazer_auth_response & IError)>;
export declare const build_url: ({ client_id, redirect_url, scopes, state }: {
    client_id: number | string;
    redirect_url: string;
    scopes: auth_scopes;
    state?: string;
}) => string;
export declare const authorize: (params: {
    code: string;
    mode?: Modes_names;
    client_id: number | string;
    client_secret: string;
    redirect_url: string;
}) => Promise<UserAuth>;
export declare const refresh_session: (params: {
    refresh_token: string;
    mode?: Modes_names;
    client_id: number | string;
    client_secret: string;
    redirect_url: string;
}) => Promise<UserAuth>;
export {};
