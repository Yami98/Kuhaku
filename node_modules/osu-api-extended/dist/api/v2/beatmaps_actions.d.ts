import { IDefaultParams, IError } from "../../types";
type params = ({
    type: 'favourite';
    beatmapset_id: number;
    add: boolean;
});
type Response<T extends params['type']> = T extends 'difficulty' ? any & IError : T extends 'difficulties' ? any & IError : IError;
declare const name: <T extends params>(params: T, addons?: IDefaultParams) => Promise<Response<T["type"]>>;
export default name;
